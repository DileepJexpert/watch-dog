name: sentinel_flutter
description: SENTINEL Monitoring Dashboard — Flutter frontend for the unified API monitoring platform.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  web_socket_channel: ^2.4.0
  stomp_dart_client: ^1.0.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  provider: ^6.1.1
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
