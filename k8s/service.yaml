apiVersion: v1
kind: Service
metadata:
  name: sentinel-backend
  namespace: monitoring
  labels:
    app: sentinel-backend
spec:
  selector:
    app: sentinel-backend
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: sentinel-frontend
  namespace: monitoring
  labels:
    app: sentinel-frontend
spec:
  selector:
    app: sentinel-frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
---
# RBAC: ServiceAccount for K8s auto-remediation operations
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sentinel-sa
  namespace: monitoring
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: sentinel-remediation-role
rules:
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets"]
    verbs: ["get", "list", "watch", "update", "patch"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: sentinel-remediation-binding
subjects:
  - kind: ServiceAccount
    name: sentinel-sa
    namespace: monitoring
roleRef:
  kind: ClusterRole
  name: sentinel-remediation-role
  apiGroup: rbac.authorization.k8s.io
